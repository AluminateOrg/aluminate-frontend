/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  eslint: {
    ignoreDuringBuilds: true,
    ignoreDuringBuilds: true,
  },
  images: { unoptimized: true },
  images: { unoptimized: true },
};

module.exports = nextConfig;
